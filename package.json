{
  "name": "tbt-org-site",
  "version": "1.0.0",
  "description": "Tomorrow's Bread Today static site built with Eleventy",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --port=8080",
    "clean": "rimraf docs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "luxon": "^3.4.4",
    "rimraf": "^5.0.5"
  }
}
