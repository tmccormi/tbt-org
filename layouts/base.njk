<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{% if title %}{{ title }} | {% endif %}Tomorrow's Bread Today</title>
  <link rel="stylesheet" href="{{ '/assets/styles.css' | url }}">
  {% if includeFirebaseUI %}
  <link rel="stylesheet" href="https://www.gstatic.com/firebasejs/ui/6.1.0/firebase-ui-auth.css">
  {% endif %}
</head>
<body>
  <header class="site-header">
    <div class="container">
      <div class="site-branding">
        <a class="site-logo" href="{{ '/' | url }}">
          <img src="{{ '/assets/images/tbt-logo.png' | url }}" alt="Tomorrow's Bread Today logo">
        </a>
        <div class="site-text">
          <h1 class="site-title"><a href="{{ '/' | url }}">Tomorrow's Bread Today</a></h1>
          <p class="tagline">Cooperative care rooted in community.</p>
        </div>
      </div>
      <nav class="site-nav">
        <ul>
          <li><a href="{{ '/' | url }}">Home</a></li>
          <li><a href="{{ '/sponsored-health-care-program/' | url }}">Sponsored Health Care Program</a></li>
          <li><a href="{{ '/service-providers/' | url }}">Service Providers</a></li>
          <li><a href="{{ '/organization-and-authority/' | url }}">Organization &amp; Authority</a></li>
          <li><a href="{{ '/blog/' | url }}">Blog</a></li>
        </ul>
      </nav>
    </div>
  </header>
  <main class="site-main">
    <div class="container">
      <article class="content">
        <h1 class="page-title">{{ title }}</h1>
        {{ content | safe }}
        {% if enableComments %}
          {% block comments %}
            {% include "partials/comments.njk" %}
          {% endblock %}
        {% endif %}
      </article>
    </div>
  </main>
  <footer class="site-footer">
    <div class="container">
      <section>
        <h2>Trustees</h2>
        <ul class="trustees">
          <li>Don McCormick, BA – Founder of USPCPs and Health Care Association Holding</li>
          <li>Bret Schulte, JD – Owner of Schulte Law Title</li>
          <li>Blair Korndorffer, AIA – Owner of Diamond Development</li>
          <li>Ken McCormick, PGA Member – Retired</li>
        </ul>
      </section>
      <p class="copyright">&copy; {{ "now" | readableDate("yyyy") }} Tomorrow's Bread Today. All rights reserved.</p>
    </div>
  </footer>
  {% set hasFirebaseConfig = env.firebaseConfig.apiKey %}
  {% set hasRecaptcha = env.recaptchaSiteKey %}
  {% set includeFirebase = (enableComments or enableFirebase) and hasFirebaseConfig %}
  {% set includeRecaptcha = (enableComments or enableRecaptcha) and hasRecaptcha %}
  {% if includeFirebase %}
  <script>
    window.__FIREBASE_CONFIG__ = {{ env.firebaseConfig | dump | safe }};
    window.__FIREBASE_FUNCTIONS_REGION__ = "{{ env.firebaseFunctionsRegion }}";
    window.__FIREBASE_EMULATORS__ = {{ env.firebaseEmulators | dump | safe }};
    window.__ALLOWED_MODERATORS__ = {{ comments.allowedModerators | dump | safe }};
  </script>
  <script type="module" src="{{ '/assets/js/firebase.js' | url }}"></script>
  {% endif %}
  {% if includeRecaptcha %}
  <script>
    window.__RECAPTCHA_SITE_KEY__ = "{{ env.recaptchaSiteKey }}";
  </script>
  <script src="https://www.google.com/recaptcha/api.js" async defer></script>
  {% endif %}
  {% if enableComments and includeFirebase %}
  <script type="module" src="{{ '/assets/js/comments.js' | url }}"></script>
  {% endif %}
  {% if includeFirebaseUI %}
  <script src="https://www.gstatic.com/firebasejs/ui/6.1.0/firebase-ui-auth.js"></script>
  {% endif %}
  {% if includeAdminComments and includeFirebase %}
  <script type="module" src="{{ '/assets/js/admin-comments.js' | url }}"></script>
  {% endif %}
</body>
</html>
